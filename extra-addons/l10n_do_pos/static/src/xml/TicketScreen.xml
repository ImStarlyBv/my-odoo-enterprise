<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="point_of_sale.TicketScreen"
       t-inherit="point_of_sale.TicketScreen"
       t-inherit-mode="extension">

        <!-- Columna NCF en el encabezado de la lista (escritorio) -->
        <xpath expr="//div[hasclass('header-row')]/div[1]" position="after">
            <t t-if="pos.config.l10n_do_is_fiscal">
                <div class="col wide p-2">NCF</div>
            </t>
        </xpath>

        <!-- Valor NCF en cada fila de orden (escritorio) -->
        <xpath expr="//div[hasclass('order-row')]/div[1]" position="after">
            <t t-if="pos.config.l10n_do_is_fiscal">
                <div class="col wide p-2">
                    <div><t t-esc="order.l10n_do_fiscal_number"/></div>
                </div>
            </t>
        </xpath>

        <!-- Botón "Devolver todo" junto al botón de regreso -->
        <xpath expr="//div[hasclass('buttons')]" position="inside">
            <t t-if="pos.config.l10n_do_is_fiscal">
                <button class="btn btn-light btn-lg text-nowrap" t-on-click="_returnAllOrder">
                    <i class="fa fa-bars me-1"/>
                    <span t-if="!ui.isSmall">Devolver todo</span>
                </button>
            </t>
        </xpath>

    </t>

</templates>
